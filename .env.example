# Local dev fallback — copy to .env at the repo root, or (preferred) copy
# server/.env.example to server/.env instead. server/index.js checks
# server/.env first and falls back to this root .env.
#
# See server/.env.example for the full list of variables.
